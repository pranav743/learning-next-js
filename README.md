# Book Management API

A RESTful API built with Node.js and Express that performs CRUD operations on books stored in a JSON file. The API supports dynamic routing for filtering books by author, year, and keyword search.
